#include <iostream>

int main() {
  int v1 = 0, v2 = 0;
  std::cin >> v1 >> v2;
  std::cout << "v1=" << v1 << std::endl;  // v1 is the first number
  std::cout << "v2=" << v2 << std::endl;  // v1 is the second number
}